apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: three-tier
spec:
  selector:
    role: api
  ports:
    - protocol: TCP
      port: 3500        # Service port (what other pods talk to)
      targetPort: 3500  # Pod containerPort
  type: ClusterIP